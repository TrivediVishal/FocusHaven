<%= form_with(model: incomplete) do |form| %>
  <% if incomplete.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(incomplete.errors.count, "error") %> prohibited this incomplete from being saved:</h2>

      <ul>
        <% incomplete.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reason, style: "display: block" %>
    <%= form.text_field :reason %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
